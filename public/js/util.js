// Generated by CoffeeScript 1.3.3
(function() {

  this.util = Object();

  this.util.attribute_function = function(att) {
    return $.fn[att] = function(val) {
      if (val != null) {
        return this.attr(att, val);
      } else {
        return this.attr(att);
      }
    };
  };

  this.util.hsl_to_hexcolor = function(h, s, l) {
    var b, g, hue, m1, m2, r;
    h = (h % 255) / 255;
    s = (s % 255) / 255;
    l = (l % 255) / 255;
    m2 = l <= 0.5 ? l * (s + 1) : l + s - l * s;
    m1 = l * 2 - m2;
    hue = function(h) {
      h = h < 0 ? h + 1 : (h > 1 ? h - 1 : h);
      if (h * 6 < 1) {
        return m1 + (m2 - m1) * h * 6;
      } else if (h * 2 < 1) {
        return m2;
      } else if (h * 3 < 2) {
        return m1 + (m2 - m1) * (2 / 3 - h) * 6;
      } else {
        return m1;
      }
    };
    r = Math.floor(hue(h + 1 / 3) * 255);
    g = Math.floor(hue(h) * 255);
    b = Math.floor(hue(h - 1 / 3) * 255);
    return "#" + ((1 << 24) + (r << 16) + (g << 8) + b).toString(16).slice(1);
  };

}).call(this);
